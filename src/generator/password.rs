use crate::config::PasswordConfig;

pub fn generate_password(config: PasswordConfig) -> String {
    String::new()
}

pub fn generate_multiple_passwords(config: PasswordConfig) -> Vec<String> {
    vec!["2".to_string()]
}
